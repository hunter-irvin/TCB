# TCB

Tournament team builder app for drafting roster and team configurations.

## Local development

1. Run `npm install`
2. Run `npm run dev`
3. Open `http://localhost:3000`

## Routes

- `/roster`
- `/teams`
